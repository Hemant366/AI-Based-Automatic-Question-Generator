services:
  - type: web
    name: ai-question-generator-backend
    env: node
    plan: free
    branch: main
    root: server
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
